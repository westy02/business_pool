<%= form_for(@subskillset) do |f| %>
  <% if @subskillset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subskillset.errors.count, "error") %> prohibited this subskillset from being saved:</h2>

      <ul>
      <% @subskillset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
